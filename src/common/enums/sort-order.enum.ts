/**
 * Sort order enum
 * @enum {string}
 * @description Sort order enum
 * @property {string} ASC - Ascending sort order
 * @property {string} DESC - Descending sort order
 */
export enum SortOrder {
  ASC = 'asc',
  DESC = 'desc',
}
